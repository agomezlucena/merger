collections.isnull=given collection can't be null
functions.hash.isnull=hash function is required to know where a collision happens and execute your business logic
functions.merger.isnull=merger function is required you need it to execute your business logic.
functions.normalizer.first.isnull=you need a normalizer function to map the first type to the result type.
functions.normalizer.second.isnull=you need a normalizer function to map the second type to the result type.
param.isnull=param: %s can't be null.
errors.exception.instantiate=can't create %s exception